services:
  yelpcamp:
    image: 40448283/yelpcamp:latest
    ports:
      - "3300:3300"
    env_file: .env            # ← easier: loads keys from your .env
    depends_on:
      - mongo
  mongo:
    image: mongo:6
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
